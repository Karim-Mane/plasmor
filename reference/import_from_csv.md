# Generate variant data frame from a CSV variant file

Generate variant data frame from a CSV variant file

## Usage

``` r
import_from_csv(variants_file)
```

## Arguments

- variants_file:

  A character with the path to the variant file

## Value

A data frame of three columns with the gene IDs, the mutation, and the
name of the drug
